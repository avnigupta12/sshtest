
export interface Roster {
    username: string;
    profileLink: string; // Assuming 'profile_link' is actually 'profileImage' as per previous discussion
    articlesCount: number;
    favoritesCount: string;
    firstArticleDate: string | null;
  }
  
